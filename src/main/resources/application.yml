spring:
  application:
    name: library
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3307/library_system
    username: root
    password: strongPassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  main:
    web-application-type: servlet

server:
  port: 3000
